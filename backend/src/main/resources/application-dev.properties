spring.application.name=backend

# H2 Database Configuration
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# UUID support for H2
spring.jpa.properties.hibernate.id.db_structure_category_delegate=uuid

# Enable quoted identifiers for H2
spring.jpa.properties.hibernate.globally_quoted_identifiers=true

# H2 Console (optional, for debugging)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# Hibernate JPA settings
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true

# Server port
server.port=8080

# spring.security.oauth2.resourceserver.jwt.issuer-uri: https://cognito-idp.us-east-2.amazonaws.com/${USER_POOL_ID}
